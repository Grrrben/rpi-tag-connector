[default]
# A unique ID for each reader
uid = str

# Credentials used to fetch a JWT
username = str
password = str

# base url of the api
api_url = http(s)://sub.domain.ext

# endpoint to fetch a JWT
endpoint_token = /api/auth/token

# endpoint to add the chipnumber
# use chipNumber and connectorUid as POST params
endpoint_chip_number = /api/external/tagconnector